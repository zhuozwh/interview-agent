# 应用显示名称，会用于 FastAPI 文档标题。
APP_NAME=Interview Agent

# 当前运行环境的标识；Phase 0 默认使用 local。
APP_ENV=local

# Python 标准日志级别。
LOG_LEVEL=INFO

# 本地 SQLite 文件路径；程序真正连接数据库时才会创建文件。
DATABASE_PATH=data/interview_agent.db
